# <项目名> - 项目规则（通用骨架模板）

> 用法：复制本文件到新项目 `.trae/rules/user_rules.md`（或其他约定的规则文件位置），把 `<尖括号>` 占位符全部替换成真实内容。
> 末尾「工作风格与协作规范」节为通用标准段，**保留原样不要删**。
> 本骨架覆盖了 AI 协作方案在项目层的全部要素，替换完即可交给 AI 开工。

## 项目简介
- 项目名：<项目名>
- 代码位置：<绝对路径>
- 目录结构：<主要子项目/模块 与 各自一句话定位，表格列出>
- 技术栈：<框架/语言/版本>

## 核心决策（<确认日期> 确认）
1. <关键方案取舍，如：二开外挂式/整体改造/动哪个不动哪个>
2. <数据库方言、部署目标、演示/上线边界 等，改动前先读本节>

## 目录角色（红线依据）
| 目录 | 内容 | 角色（可写/只读） |
|---|---|---|
| <主代码目录> | <说明> | 可自由修改 |
| <原系统/只读目录> | <说明> | **绝对只读，禁止修改任何文件**（若有） |
| <需求文档目录> | <说明> | 需求依据，开发前先读 |

## 关键约定
- 只做代码层开发/审核/优化建议：不打包、不部署、不启动服务、不测试接口、不执行 SQL（DDL 只出脚本，执行权在用户）
- 严禁乱改配置文件（application.yml 等），如需修改先询问确认
- 涉及生产/现场/线上的改动，改前先 git 备份/确认版本
- 改代码前先读现有代码 → 给方案 → 用户确认后再改
- 不确定信息标注「待确认」，绝不编造字段名/表名/接口名/数据

## 文档落盘规范
- **唯一落盘根目录**：<docs 根，建议 docs/ 或项目约定目录，禁止写进 src/main/resources/ 等会被打包的目录>
- 子目录分类（按主题归位）：
  | 子目录 | 放什么 |
  |---|---|
  | `analysis/` | 需求分析、方案评估、差距清单 |
  | `database/` | 数据库表清单、DDL、表结构导出 |
  | `design/` | 接口设计、架构设计、模块设计 |
  | `devlog/` | 开发日志、进度记录、决策记录、审查报告 |
- 文件名规范：`主题_日期.md`（如 `XX模块设计_20260903.md`），避免重名覆盖
- **文档索引（强制）**：docs 根建 `00-文档索引.md`（编号 00 排最前），作为全目录唯一入口索引。每次落盘新文档/更新文档后**必须同步**更新索引（文件名/日期/状态/一句话用途）与头部「最近更新」
- **文档头部元信息（新文档强制）**：标题下写 `日期 / 分类 / 状态（现行·历史·后置）/ 关联文件` 四行

## 当前进度
- <最近完成项 / 进行中项 / 待办项，按模块或里程碑列>

## 关键环境信息
- 服务端口/地址：<如 8080，测试 http://... 生产 http://...>
- 数据库：<类型/库名/双数据源说明>
- 工作流：<如：先本地测试 → 确认 → 再部署现场/生产>
- 其他：<JWT 密钥位置、特殊注意事项等，敏感信息只写"见配置文件"不写明文>

## 工作风格与协作规范（统一标准段，保留不动）
### 汇报格式（每次回复默认遵守）
1. 结论先行：第一句给结论/判断，再展开依据
2. 表格 + 阿拉伯数字编号组织内容（禁罗马数字/汉字编号）
3. 数值保留两位小数；禁止"开发中"等未完成字眼（已完成/待办/阻塞说清）
4. 收尾三件套：每次任务结束必须输出「改动文件清单 + 自检结论 + 下一步建议」

### 默认开发节奏（除非用户另有指示）
1. 开工先读：本规则文件 + 最新进度/日志文件，对齐上下文再动手
2. 方案先行：新功能/新表/新接口，先出方案落盘到 docs 约定目录，等用户确认再编码
3. 编码对齐：与项目已交付模块的分层与写法保持一致，不发明新风格
4. 交付自检：编译级零错误 + 红线自查 + 收尾三件套
5. 汇报附改动文件清单与行号，不夸大、不省略

### 红线（最高优先级）
1. 不打包、不部署、不启动服务、不测试接口、不执行 SQL（DDL 只出脚本）
2. 严禁乱改配置文件，如需修改先征求同意
3. 只读目录绝对只读（见上文「目录角色」）
4. 涉及生产/线上的改动，改前先 git 备份/确认版本
5. 不确定信息标注「待确认」，绝不编造

### Subagent 协作（用户会 @ 调用，subagent 文件见 .trae/agents/）
| Subagent | 用途 | 调用时机 |
|---|---|---|
| @architect | 架构分析/方案设计/结构评审 | 新任务开工、跨模块改造前 |
| @code-reviewer | 独立深度审查（P0/P1/P2 问题清单） | 模块/功能点交付后 |
| @doc-keeper | 文档结构合规检查、归档建议、**维护 00-文档索引** | 新文档落盘前、docs/ 混乱时、复盘查询资料时 |
| @requirement-clarifier | 需求澄清（先问清再动工） | 新需求/含糊任务动工前 |
- 收到 P0/P1 修复清单时：只出修复方案，用户确认后才动手；只改问题文件，不顺手重构
